Check why the layout of html files in this site is broken (localhost:8000), it seem we have many 404 file. 
You can always comparing by run the web server of /Users/ducph/Projects/lavatile/ to see how it should work. But check for 404 code first.
